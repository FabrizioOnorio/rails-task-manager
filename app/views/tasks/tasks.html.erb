<h1>My Tasks</h1>

<ul>
<% @tasks.each do |task| %>
<li>
<% if task.completed %>
  <i class="far fa-check-square"></i>
<% else %>
  <i class="far fa-square"></i>
<% end %>

<%= link_to task.title, task_path(task.id) %>
<% end %>
</li>
</ul>

<%= link_to 'Create a task', '/tasks/new' %>
